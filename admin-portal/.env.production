# Admin Portal - Production Environment Variables
# Loaded by Vite during production builds (vite build).
# Safe to commit - no secrets.
#
# IMPORTANT: Set VITE_API_BASE_URL in Railway/Vercel dashboard environment variables.
# This file is a fallback for local production builds only.
# Do NOT hardcode the Railway domain here — it has changed before and may change again.

VITE_API_BASE_URL=
