{
  "name": "ethereal-scholar-erp",
  "version": "1.0.0",
  "description": "Ethereal Scholar - Student ERP Portal",
  "scripts": {
    "start": "node start.js",
    "build": "node scripts/generate-config.js && cd backend && npx prisma generate",
    "backend": "cd backend && node server.js",
    "frontend": "node frontend-server.js",
    "db:setup-pg": "cd backend && npm run db:setup-pg",
    "db:setup-sqlite": "cd backend && npm run db:setup-sqlite",
    "postinstall": "npm install --prefix backend --ignore-scripts"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "dependencies": {
    "@capacitor/browser": "^6.0.0",
    "@capacitor/push-notifications": "^6.0.0",
    "dotenv": "^17.4.2",
    "@prisma/client": "5.15.0",
    "prisma": "5.15.0"
  }
}
